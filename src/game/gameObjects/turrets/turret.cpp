#include "turret.hpp"

void Turret::update()
{
    /*std::erase_if(bullets, [](const BasicBullet& b)->bool{return b.shouldDelete();});
    for(auto& bullet : bullets)
    {
        bullet.update();
        bullet.hitTarget();
    }*/
}

